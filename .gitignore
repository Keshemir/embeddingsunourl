# env / secrets
.env
.env.local

# python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# data / artifacts
# `data/index/` (audio.npy + tracks.parquet + calibration.json) is allowed
# in git for now — small, useful for sharing across machines. Migrate to
# git-lfs or external storage when corpus grows past ~10 MB.
data/audio/
data/cache/

# logs
*.log

# os / editor
.DS_Store
.ipynb_checkpoints/
.idea/
.vscode/

# Claude Code workspace
.claude/

# unrelated files in this directory (resume / scratch)
resume_shaimanova.docx
build_resume.js
